---
name: Deploy to Production (Fixed)

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        env:
          VM_IP: ${{ secrets.VM_IP }}
        run: |
          echo "🚀 Deploying to production server..."
          
          # Create deployment package
          tar czf deploy.tar.gz docker-compose.yml backend/ frontend/
          
          # Deploy in a single SSH session
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 ubuntu@$VM_IP '
            set -e
            
            echo "🏗️ Setting up deployment environment..."
            sudo mkdir -p /opt/helpdesk-ticketing-system
            sudo chown ubuntu:ubuntu /opt/helpdesk-ticketing-system
            cd /opt/helpdesk-ticketing-system
            
            # Stop existing containers
            if [ -f "docker-compose.yml" ]; then
              echo "🛑 Stopping existing containers..."
              sudo docker-compose down 2>/dev/null || echo "No containers to stop"
            fi
            
            # Clean directory
            echo "🧹 Cleaning deployment directory..."
            rm -rf ./* 2>/dev/null || true
            
            # Install Docker if needed
            if ! command -v docker &> /dev/null; then
              echo "📥 Installing Docker..."
              sudo apt-get update -qq
              sudo apt-get install -y docker.io docker-compose
              sudo systemctl enable docker
              sudo systemctl start docker
              echo "✅ Docker installed"
            else
              echo "✅ Docker already available"
            fi
          '
          
          echo "📤 Copying files to server..."
          scp -o StrictHostKeyChecking=no -o ConnectTimeout=30 \
            deploy.tar.gz ubuntu@$VM_IP:/opt/helpdesk-ticketing-system/
          
          echo "🏗️ Extracting and starting services..."
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 ubuntu@$VM_IP '
            set -e
            cd /opt/helpdesk-ticketing-system
            
            # Extract files
            tar xzf deploy.tar.gz
            rm deploy.tar.gz
            
            # Verify docker-compose.yml exists
            if [ ! -f "docker-compose.yml" ]; then
              echo "❌ docker-compose.yml not found!"
              exit 1
            fi
            
            echo "📋 Files in deployment directory:"
            ls -la
            
            # Create environment file
            echo "🔧 Creating environment variables..."
            echo "NODE_ENV=production" > .env
            echo "PORT=3001" >> .env
            echo "DB_HOST=postgres" >> .env
            echo "DB_PORT=5432" >> .env
            echo "DB_NAME=helpdesk_db" >> .env
            echo "DB_USER=postgres" >> .env
            echo "DB_PASSWORD=helpdesk_demo_password_2024" >> .env
            echo "JWT_SECRET=demo_jwt_secret_key_for_testing_only_change_in_production_a1b2c3d4e5f6789012345678901234567890" >> .env
            echo "SESSION_TIMEOUT=3600000" >> .env
            echo "MAX_LOGIN_ATTEMPTS=5" >> .env
            echo "LOCKOUT_TIME=900000" >> .env
            
            echo "✅ Environment variables created:"
            cat .env
            
            # Clean up any existing containers
            echo "🧹 Cleaning up existing containers..."
            sudo docker system prune -f 2>/dev/null || true
            
            # Start services with verbose output
            echo "🚀 Starting Docker Compose services..."
            sudo docker-compose up -d --build
            
            echo "⏱️ Waiting for containers to initialize..."
            sleep 15
            
            echo "📊 Final container status:"
            sudo docker-compose ps
            sudo docker ps -a
          '

      - name: Comprehensive health check
        env:
          VM_IP: ${{ secrets.VM_IP }}
        run: |
          echo "🏥 Running comprehensive health check..."
          
          # Wait for services to fully start
          sleep 45
          
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 ubuntu@$VM_IP '
            cd /opt/helpdesk-ticketing-system
            
            echo "=== CONTAINER STATUS ==="
            sudo docker-compose ps
            
            echo ""
            echo "=== ALL DOCKER CONTAINERS ==="
            sudo docker ps -a
            
            echo ""
            echo "=== BACKEND LOGS ==="
            sudo docker logs helpdesk-backend --tail 30 2>&1 || echo "Backend container not found"
            
            echo ""
            echo "=== FRONTEND LOGS ==="
            sudo docker logs helpdesk-frontend --tail 30 2>&1 || echo "Frontend container not found"
            
            echo ""
            echo "=== POSTGRESQL LOGS ==="
            sudo docker logs helpdesk-postgres --tail 30 2>&1 || echo "PostgreSQL container not found"
            
            echo ""
            echo "=== NETWORK CHECK ==="
            sudo apt-get install -y net-tools 2>/dev/null || true
            sudo netstat -tlnp 2>/dev/null | grep -E ":300[01]|:5432" || echo "No services on expected ports"
            
            echo ""
            echo "=== INTERNAL HEALTH CHECKS ==="
            sudo docker exec helpdesk-backend wget -q --spider http://localhost:3001/health && echo "✅ Backend internal health OK" || echo "❌ Backend internal health failed"
            sudo docker exec helpdesk-frontend wget -q --spider http://localhost:3000/health && echo "✅ Frontend internal health OK" || echo "❌ Frontend internal health failed"
            sudo docker exec helpdesk-postgres pg_isready -U postgres && echo "✅ PostgreSQL ready" || echo "❌ PostgreSQL not ready"
          '
          
          echo ""
          echo "=== EXTERNAL HEALTH CHECKS ==="
          curl -f -m 10 http://$VM_IP:3000/health && echo "✅ Frontend externally accessible" || echo "❌ Frontend not accessible"
          curl -f -m 10 http://$VM_IP:3001/health && echo "✅ Backend externally accessible" || echo "❌ Backend not accessible"
